# 논문 리더 프로젝트 — 개발 계획서

다운받은 논문 PDF를 "책처럼" 읽는 로컬 프로그램. 세분화된 목차, 페이지 단위 글자 양 조절, 영어 논문 번역(토글), Kindle 스타일 UI. 코드는 GitHub에 공개하고 각자 로컬에서 실행하는 방식.

---

## 1. 확정된 결정 사항

| 항목 | 결정 |
|------|------|
| 배포 | 코드만 GitHub 공개, 로컬 실행 (`python app.py` → localhost) |
| PDF 파싱 | **Docling** (IBM, 무료 오픈소스) — 2단 컬럼·표·그림·읽기순서 자동 정리 |
| 번역 | **DeepL API Free** (월 50만 자) — 번역 계층은 함수로 추상화해 교체 가능하게 |
| 수식 | **KaTeX** (무료) 렌더링 |
| 그림·표 | 만나면 **한 쪽(페이지) 통째로** 배치, 캡션 포함 |
| 인용 | 책처럼 **페이지 하단 각주** 방식 ([12] → 해당 페이지 아래 표시) |
| 읽기 UI | **Kindle 스타일** — 페이지 넘김, 진행률 바, 북마크, 하이라이트 |
| 번역 표시 | **토글 전환** (원문 ↔ 번역, 나란히 X) |
| 저장 | SQLite (읽기 상태·북마크·하이라이트·라이브러리) |

---

## 2. 전체 아키텍처

```
[PDF 논문]
   │
   ▼
┌─────────────── 전처리 파이프라인 (Python, 로컬 1회 실행) ───────────────┐
│  Docling 파싱  →  DeepL 번역(+캐싱)  →  메타데이터 추출(제목·저자·연도)  │
└──────────────────────────────────┬──────────────────────────────────┘
                                    ▼
                          [ 중간 JSON 파일 ]
              (섹션·문단·그림·표·수식·번역·참고문헌을 구조화)
                                    │
                                    ▼
┌─────────────────── 로컬 뷰어 (웹앱, Flask/FastAPI) ────────────────────┐
│  페이지네이션 엔진 · 세분화 목차 · 번역 토글 · 각주형 인용             │
│  KaTeX 수식 · 읽기 상태(북마크·하이라이트) · 라이브러리(책장) · 검색   │
└────────────────────────────────────────────────────────────────────────┘
```

핵심 아이디어: **무거운 처리(파싱·번역)는 전처리에서 1회만**, 뷰어는 가공된 JSON만 읽어서 가볍게 표시. 논문을 한 번 넣으면 그 결과가 저장돼서 다음부턴 즉시 열림.

---

## 3. 기술 스택

| 레이어 | 도구 | 비고 |
|--------|------|------|
| 언어 | Python 3.12 | 이미 익숙한 환경 |
| PDF 파싱 | `docling` | `pip install docling` |
| 번역 | `deepl` (공식 파이썬 SDK) | Free 키 필요, `.env` 관리 |
| 웹 서버 | FastAPI 또는 Flask | 로컬 웹앱 띄우기 |
| 저장 | SQLite (`sqlite3` 내장) | 별도 설치 불필요 |
| 프론트 | HTML/CSS/JS (바닐라 or 가벼운 프레임워크) | Kindle 스타일 뷰어 |
| 수식 | KaTeX (CDN) | 브라우저에서 렌더 |
| 메타데이터 | arXiv/CrossRef API | 제목·저자·연도 자동 |

---

## 4. 중간 JSON 스키마 (초안)

전처리와 뷰어를 잇는 계약. 이걸 먼저 확정해야 양쪽을 독립적으로 개발할 수 있음.

```json
{
  "meta": {
    "title": "논문 제목",
    "authors": ["저자1", "저자2"],
    "year": 2024,
    "source_pdf": "paper.pdf",
    "lang": "en"
  },
  "toc": [
    { "id": "sec1", "title": "Introduction", "level": 1, "children": [] }
  ],
  "blocks": [
    {
      "id": "b001",
      "type": "paragraph",        // paragraph | heading | figure | table | equation | reference
      "section_id": "sec1",
      "text_original": "원문...",
      "text_translated": "번역문...",
      "citations": ["ref12"]       // 이 블록이 참조하는 참고문헌 id
    },
    {
      "id": "b002",
      "type": "figure",
      "image_path": "figures/fig1.png",
      "caption_original": "Figure 1: ...",
      "caption_translated": "그림 1: ...",
      "full_page": true            // 한 쪽 통째로
    }
  ],
  "references": [
    { "id": "ref12", "text": "[12] Author et al. ..." }
  ]
}
```

---

## 5. 단계별 개발 계획 (권장 순서)

### Phase 0 — 프로젝트 뼈대
- [ ] GitHub 저장소 생성, README 초안, `.gitignore`(`.env` 제외)
- [ ] 폴더 구조: `pipeline/`, `viewer/`, `data/`, `schema/`
- [ ] `.env.example` 작성 (DeepL 키 자리)
- [ ] **중간 JSON 스키마 확정** ← 가장 먼저. 이후 모든 작업의 기준

### Phase 1 — 전처리 파이프라인
- [ ] Docling으로 PDF → 구조화 데이터 추출
- [ ] Docling 결과를 우리 JSON 스키마로 변환
- [ ] 그림·표 이미지 별도 저장 + 경로 연결
- [ ] 참고문헌 목록 파싱, 본문 인용([12])과 id 매핑
- [ ] 수식을 KaTeX가 읽을 형태로 보존
- [ ] DeepL 번역 함수 구현 (수식·기호는 제외 태그 처리, 캐싱)
- [ ] 메타데이터 추출 (arXiv/CrossRef)

### Phase 2 — 뷰어 기본 골격
- [ ] Flask/FastAPI로 localhost 서버 + JSON 로드
- [ ] 세분화 목차 렌더링 + 클릭 이동
- [ ] KaTeX 수식 렌더링
- [ ] **페이지네이션 엔진** (가장 어려운 부분 — 아래 6번 참고)
- [ ] 그림·표 한 쪽 통째로 배치 규칙

### Phase 3 — Kindle 스타일 읽기 경험
- [ ] 페이지 넘김 UI (좌우, 키보드 방향키)
- [ ] 진행률 바
- [ ] 번역 토글 (원문 ↔ 번역)
- [ ] 각주형 인용 (본문 [12] → 페이지 하단)
- [ ] 하이라이트 + 메모
- [ ] 마지막 읽던 위치 저장/복원 (SQLite)

### Phase 4 — 라이브러리 & 마무리
- [ ] 여러 논문 목록(책장) 화면
- [ ] 전체 텍스트 검색
- [ ] README 정리 (설치·실행법), 스크린샷
- [ ] 예시 논문 하나로 데모

---

## 6. 미리 알아둘 난이도 (과소평가 금지 지점)

**페이지네이션 엔진** — 이 프로젝트에서 손이 제일 많이 감. "한 장에 들어가는 글자 양 조절"이 곧 이것. 텍스트가 화면 높이를 넘으면 자동으로 다음 페이지로 끊는 리플로우 로직 필요. 여기에 "그림·표는 한 쪽 통째로", "각주는 페이지 하단" 규칙까지 얹힘.

- 방법 A: 렌더링 후 높이를 재서 넘칠 때 끊기 (정확·복잡)
- 방법 B: CSS 컬럼/다단 기능에 맡기기 (간단·제어 덜 정밀)
- 권장: B로 프로토타입 → 부족하면 A로 정교화

**PDF 파싱 품질** — Docling이 좋지만 수식이 매우 많은 논문은 완벽하지 않을 수 있음. 처음엔 Docling만으로, 나중에 부족하면 Nougat(수식→LaTeX 특화)을 보조로 붙이는 확장 여지를 남겨둘 것.

**번역 시 수식 보존** — DeepL에 LaTeX를 그냥 던지면 깨짐. DeepL의 "번역 제외 태그" 기능으로 수식·변수·고유명사를 감싸 보호. 그림·표의 캡션 텍스트는 번역 대상.

---

## 7. 다음에 정하면 좋을 것들 (지금은 미뤄도 됨)

- 목차 "세분화"의 구체적 의미: Docling 섹션 그대로 쓸지, LLM으로 더 잘게 쪼갤지
- 폰트·글자 크기·다크모드 등 읽기 환경 설정 범위
- 하이라이트/메모를 내보내기(export)할지 여부
